// TODO: replace this placeholder with real routing (BrowserRouter, AppShell,
// ProtectedRoute, the Toaster) once Login and auth/session.js exist - see the
// real project's App.jsx for the exact shape to match.
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-slate-50 text-slate-500">
      <p className="text-sm">EAD-PROJECT skeleton — routing not wired up yet.</p>
    </div>
  )
}
